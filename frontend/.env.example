# URL of your deployed backend (no trailing slash, no /api suffix)
# Local dev: leave unset, it defaults to http://localhost:8000
# Production (Vercel): set this in Project Settings -> Environment Variables
VITE_API_URL=https://your-backend.onrender.com
